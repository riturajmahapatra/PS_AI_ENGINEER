# Environment variables / API keys
.env
.env.local
.env.*.local

# Run outputs: worktrees, ledgers, transcripts, reports.
# These are evidence, not source. Never commit them.
.artifacts/
*.log

# The demo target repo is its own git repo; keep its history out of ours.
examples/target-repo/.git/

# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.uv/

# Test and type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.json
htmlcov/

# OS / editor / local tool settings
.claude/settings.local.json
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build outputs
dist/
build/
*.egg-info/
